
public class Helper {
	//Helper class with methods that are used often in several algorithms
	//swap is an example

	//array elements swapping method stolen from stackoverflow
	public static void swap(int i, int j, int[] arr) {
		   int t = arr[i];
		   arr[i] = arr[j];
		   arr[j] = t;
		}
	
}
